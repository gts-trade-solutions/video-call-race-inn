# ---------- MySQL ----------
MYSQL_HOST="127.0.0.1"
MYSQL_PORT="3306"
MYSQL_USER="root"
MYSQL_PASSWORD="race@123"
DB_NAME=video_call_tool

# ---------- Auth ----------
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=replace-with-a-long-random-string

# ---------- LiveKit ----------
# Get free credentials at https://cloud.livekit.io (or self-host).
# LIVEKIT_URL is the wss:// server url. The NEXT_PUBLIC_ copy is used by the browser.
LIVEKIT_API_KEY=your_api_key
LIVEKIT_API_SECRET=your_api_secret
LIVEKIT_URL=wss://your-project.livekit.cloud
NEXT_PUBLIC_LIVEKIT_URL=wss://your-project.livekit.cloud

# ---------- Recording → Amazon S3 (optional) ----------
# Enables the in-call "Record" button. Meetings are recorded by LiveKit Egress
# (server-side, room composite MP4) and uploaded straight to this S3 bucket.
# Egress must be available: it's built in on LiveKit Cloud; self-hosters must run
# the egress service (https://docs.livekit.io/home/self-hosting/egress/).
AWS_S3_BUCKET_NAME=your-recordings-bucket
AWS_S3_REGION=us-east-1
AWS_S3_ACCESS_KEY_ID=your_aws_access_key_id
AWS_S3_SECRET_ACCESS_KEY=your_aws_secret_access_key
NEXT_PUBLIC_S3_BUCKET_URL=https://your-recordings-bucket.s3.us-east-1.amazonaws.com/
# Optional — for S3-compatible stores (MinIO, Cloudflare R2, Wasabi, …):
# S3_ENDPOINT=https://s3.example.com
# S3_FORCE_PATH_STYLE=true

# ---------- Email / password reset (optional) ----------
# For "Forgot password" emails (a 4-digit code). If unset, in dev the code is
# shown on screen instead of emailed. Two backends — pick ONE:
#
# 1) Amazon SES (reuses the S3 AWS creds above; SES_FROM must be a verified SES
#    identity, and the account should be out of the SES sandbox to email anyone):
# SES_FROM="Race Meetings <no-reply@yourdomain.com>"
# AWS_SES_REGION=ap-south-1
#    (optional dedicated keys: AWS_SES_ACCESS_KEY_ID / AWS_SES_SECRET_ACCESS_KEY)
#
# 2) SMTP (Gmail: enable 2FA, create an App Password):
# SMTP_SERVICE=gmail
# SMTP_USER=you@gmail.com
# SMTP_PASS=your_16_char_app_password
# SMTP_FROM="Race Meetings <you@gmail.com>"
# --- or any SMTP host instead of SMTP_SERVICE ---
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false

# ---------- Calendar (optional) ----------
# The public URL of the app, used to build absolute join links inside calendar
# invites and OAuth redirects. Set this behind a proxy; otherwise it's derived
# from the request. e.g. https://meet.yourdomain.com
NEXT_PUBLIC_APP_URL=

# Google OAuth powers BOTH "Sign in with Google" and Google Calendar sync.
# Create one OAuth client (type: Web application) at
# https://console.cloud.google.com/apis/credentials and register BOTH redirect
# URIs below on it. (For Calendar sync also enable the Google Calendar API.)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Each must EXACTLY match a redirect URI registered on the OAuth client:
GOOGLE_SIGNIN_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3000/api/calendar/google/callback
